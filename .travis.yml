
sudo: require
language: cpp
compiler: gcc

before_install:
  - add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - apt-get update -qq
  - export CODECOV_TOKEN="c616a2e1-a106-49fd-81c7-fd6c7f66d904"

install: 
  - apt-get install -qq g++-5
  - update-alternatives --install /usr/bin/gcov gcov /usr/bin/gcov-5 90

script: 
  - ./build.sh
  - ./travis_qmake_gcc_cpp11_gcov
  - ./get_code_cov.sh

after_success: 
  - bash <(curl -s https://codecov.io/bash)